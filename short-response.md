# Short response questions

1. each classmate should do git add ., git commit -m,and git push to save the work and they should check if there are any changes in main

2. Developers use branches to test out something new without making any changes to main

3. git is a tool which helps developers look at the changes made in the code, while github is where storage is to keep the code,they work together as github stores the code you made and git allows you to look at the changes you have made to that code.

4. The commands are in the incorrect order, the correct command sequence is git add . git commit -m and git push. 